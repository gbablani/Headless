# Optional: All credentials can be prompted interactively if not set
# If you prefer to store credentials in .env, uncomment and fill the values below

APP_ENTRY_URL=https://portal.microsofticm.com/imp/
APP_USER=user@contoso.com
APP_PASSWORD=Passw0rd!
MFA_MODE=TOTP
TOTP_SECRET=BASE32SECRET

# Optional: Azure Blob Storage for uploading scrape results
AZURE_STORAGE_CONNECTION_STRING=
AZURE_BLOB_CONTAINER=intranet-scrapes
